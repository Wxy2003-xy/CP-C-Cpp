#include <iostream>
#include <ctype.h>

int generate_ALUop_signal(uint8_t* opcode, uint8_t* alu_op);
int generate_ALUCntrol_signal(uint8_t* opcode, uint8_t* func, uint8_t* alu_control);

int main() {

    return 0;
}